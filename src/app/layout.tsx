import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "SilkRoad Health - World-Class Healthcare in Beijing at 70% Less",
  description: "Access top-tier Beijing hospitals for cardiac surgery, neurosurgery, oncology, IVF and more. Save 60-85% compared to US/UK prices with world-renowned specialists.",
  keywords: "medical tourism, Beijing hospitals, healthcare China, heart surgery Beijing, cancer treatment China, IVF Beijing, medical travel",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
